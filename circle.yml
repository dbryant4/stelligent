# circle.yml
machine:
  python:
    version: 2.7
  services:
    - docker

dependencies:
  pre:
    - pip install git+git://github.com/dbryant4/molecule.git@docker-ports

test:
  override:
    - molecule test
